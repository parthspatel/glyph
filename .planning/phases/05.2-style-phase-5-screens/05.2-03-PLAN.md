---
phase: 05.2-style-phase-5-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/ProjectCreatePage.tsx
  - apps/web/src/pages/ProjectEditPage.tsx
  - apps/web/src/components/project/ProjectForm.tsx
  - apps/web/src/components/project/ProjectChecklist.tsx
  - apps/web/src/components/project/sections/BasicInfoSection.tsx
autonomous: true

must_haves:
  truths:
    - "Project create/edit pages use proper header styling with icon"
    - "Form accordion sections use design tokens for expand/collapse"
    - "Form inputs use shadcn Input component or proper Tailwind styling"
    - "Buttons use shadcn Button component"
    - "Checklist sidebar has progress indicator and styled section list"
    - "Loading and error states use Skeleton and proper error styling"
    - "Tag input has pill-style tags with remove button"
  artifacts:
    - path: "apps/web/src/pages/ProjectCreatePage.tsx"
      provides: "Styled project creation page"
      contains: "bg-card|Button|text-foreground"
    - path: "apps/web/src/pages/ProjectEditPage.tsx"
      provides: "Styled project edit page with loading state"
      contains: "bg-card|Button|Skeleton"
    - path: "apps/web/src/components/project/ProjectForm.tsx"
      provides: "Styled accordion form with proper triggers"
      contains: "bg-card|border|text-foreground"
    - path: "apps/web/src/components/project/ProjectChecklist.tsx"
      provides: "Styled checklist sidebar with progress"
      contains: "bg-card|text-success|text-muted-foreground"
    - path: "apps/web/src/components/project/sections/BasicInfoSection.tsx"
      provides: "Styled form section with inputs"
      contains: "border-input|bg-background|rounded-md"
  key_links:
    - from: "ProjectForm.tsx"
      to: "@/components/ui/button"
      via: "import"
      pattern: "import.*Button.*from"
---

<objective>
Style the Project Form/Wizard feature (ProjectCreatePage, ProjectEditPage, ProjectForm, ProjectChecklist, BasicInfoSection) using Tailwind classes and design tokens from Phase 3.1.

Purpose: Create a polished project creation/edit experience with styled accordion sections, proper form inputs, and a clear checklist sidebar showing progress.

Output: Styled form components using bg-card, proper input styling, shadcn buttons, and a progress-tracking checklist sidebar.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.2-style-phase-5-screens/05.2-CONTEXT.md
@apps/web/src/index.css
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/src/pages/admin/ProjectTypesPage.tsx (reference for page structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style ProjectCreatePage and ProjectEditPage</name>
  <files>apps/web/src/pages/ProjectCreatePage.tsx, apps/web/src/pages/ProjectEditPage.tsx</files>
  <action>
**ProjectCreatePage.tsx:**

1. **Page container**: Replace .page-container with max-w-5xl mx-auto p-6 space-y-6

2. **Page header**: 
   Use header with flex items-center gap-3
   - FolderPlus icon from lucide-react with size-8 text-primary
   - div with:
     - h1: text-2xl font-bold text-foreground "Create Project"
     - p: text-muted-foreground "Set up a new annotation project"

Import FolderPlus from "lucide-react".

**ProjectEditPage.tsx:**

1. **Page container**: max-w-5xl mx-auto p-6 space-y-6

2. **Loading state**: Use Skeleton components
   - Header skeleton: flex items-center gap-3
     - Skeleton size-8 rounded (icon placeholder)
     - div with Skeleton h-7 w-48 and Skeleton h-4 w-64 mt-1
   - Form skeleton: bg-card rounded-lg border p-6
     - Skeleton h-10 w-full for each form section (5 items)
     - Skeleton h-10 w-32 mt-4 for button

3. **Error state**: bg-card rounded-lg border p-8 text-center
   - AlertCircle icon with size-12 mx-auto mb-4 text-muted-foreground
   - h2 with text-lg font-semibold text-foreground "Project Not Found"
   - p with text-muted-foreground mt-2
   - Button onClick to navigate back, mt-4

4. **Page header** (when loaded):
   Use header with flex items-center gap-3
   - Pencil icon from lucide-react with size-8 text-primary
   - div with:
     - h1: text-2xl font-bold text-foreground "Edit Project"
     - p: text-muted-foreground showing project.name

Import Skeleton from "@/components/ui/skeleton", Button from "@/components/ui/button", AlertCircle, Pencil from "lucide-react".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View /projects/new - header has FolderPlus icon. View /projects/:id/edit - loading shows skeletons, error shows centered card, loaded shows Pencil icon with project name.</verify>
  <done>ProjectCreatePage and ProjectEditPage have styled headers with icons, skeleton loading states, and proper error states. No custom CSS classes.</done>
</task>

<task type="auto">
  <name>Task 2: Style ProjectForm accordion and actions</name>
  <files>apps/web/src/components/project/ProjectForm.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities:

1. **Form layout**: Replace .project-form with flex gap-6

2. **Main form area**: Replace .project-form-main with flex-1 space-y-4

3. **Accordion container**: Replace .project-accordion with space-y-2

4. **AccordionSection component styling**:

   a. **Accordion item**: Replace .accordion-item with bg-card rounded-lg border overflow-hidden

   b. **Accordion header**: Replace .accordion-header with border-b border-border (only when open)

   c. **Accordion trigger**: Replace .accordion-trigger with:
      w-full px-4 py-3 flex items-center justify-between text-left hover:bg-muted/50 transition-colors
      
   d. **Trigger content**: Replace .accordion-trigger-content with flex items-center gap-3

   e. **Status indicator**: Replace .accordion-status with:
      - Container: size-6 rounded-full flex items-center justify-center text-sm font-medium
      - Complete state: bg-success/10 text-success
      - Error state: bg-destructive/10 text-destructive  
      - Default state: bg-muted text-muted-foreground
      - Use Check icon for complete, AlertCircle for error, Circle for default (from lucide-react, size-4)

   f. **Accordion title**: Replace .accordion-title with font-medium text-foreground

   g. **Chevron**: Replace .accordion-chevron with:
      - Use ChevronDown icon from lucide-react with size-4 text-muted-foreground
      - Add transition-transform duration-200
      - When open: rotate-180

   h. **Accordion content**: Replace .accordion-content with px-4 py-4

   i. **Content inner**: Replace .accordion-content-inner with (remove or keep as-is, content styling is in sections)

5. **Form actions**: Replace .project-form-actions with flex items-center justify-end gap-3 pt-4 border-t border-border

6. **Cancel button**: Use Button variant="ghost"

7. **Submit button**: Use Button (default variant)
   - Show loading state with disabled and "Saving..." text

Import Button from "@/components/ui/button", Check, AlertCircle, Circle, ChevronDown from "lucide-react", cn from "@/lib/utils".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectForm - accordion items have card backgrounds, triggers show completion status with icons, chevron rotates on expand, action buttons are styled.</verify>
  <done>ProjectForm has styled accordion with card backgrounds, status indicators with icons, smooth chevron rotation, and shadcn action buttons.</done>
</task>

<task type="auto">
  <name>Task 3: Style ProjectChecklist sidebar</name>
  <files>apps/web/src/components/project/ProjectChecklist.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities:

1. **Checklist aside**: Replace .project-checklist with w-64 shrink-0

2. **Sticky wrapper**: Replace .checklist-sticky with sticky top-6 space-y-4

3. **Section cards**: Replace .checklist-section with bg-card rounded-lg border p-4

4. **Section titles**: Replace .checklist-title with text-sm font-semibold text-foreground mb-3

5. **Progress section**:
   a. **Progress container**: Replace .checklist-progress with space-y-2
   b. **Progress bar track**: h-2 bg-muted rounded-full overflow-hidden
   c. **Progress bar fill**: h-full bg-primary transition-all duration-300
   d. **Progress label**: text-sm text-muted-foreground text-right

6. **Section checklist**:
   a. **List**: Replace .checklist-items with space-y-1
   b. **Item button**: Replace .checklist-item with:
      w-full flex items-center gap-2 px-2 py-1.5 rounded-md text-sm text-left transition-colors hover:bg-muted
   c. **Complete state**: text-foreground
   d. **Incomplete state**: text-muted-foreground

7. **Checklist icons**: Replace .checklist-icon with size-4 shrink-0
   - Complete: Use Check icon with text-success
   - Incomplete: Use Circle icon with text-muted-foreground

8. **Checklist label**: Replace .checklist-label with truncate

9. **Activation requirements**:
   a. **Static item**: Replace .checklist-item.static with:
      flex items-center gap-2 px-2 py-1.5 text-sm
   b. **Met requirement**: text-success
   c. **Unmet requirement**: text-muted-foreground
   d. **Go link**: Replace .checklist-link with:
      ml-auto text-xs text-primary hover:underline

10. **Activation status messages**:
    a. **Ready message**: Replace .activation-ready with:
       text-sm text-success font-medium flex items-center gap-1.5 mt-3
       Add CheckCircle icon with size-4
    b. **Note message**: Replace .activation-note with:
       text-xs text-muted-foreground mt-3

11. **Help section**: Replace .checklist-help with:
    bg-muted/50 rounded-lg p-3 text-xs text-muted-foreground
    - Strong tag: font-medium text-foreground
    - kbd tag: px-1.5 py-0.5 bg-background rounded border text-xs font-mono

Import Check, Circle, CheckCircle, X from "lucide-react", cn from "@/lib/utils".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectChecklist - progress bar shows purple fill, section items show check/circle icons with proper colors, activation status is styled, help section has proper kbd styling.</verify>
  <done>ProjectChecklist has styled progress bar, section list with icons, activation requirements with status colors, and help section with kbd styling.</done>
</task>

<task type="auto">
  <name>Task 4: Style BasicInfoSection form fields</name>
  <files>apps/web/src/components/project/sections/BasicInfoSection.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities:

1. **Section container**: Keep space-y-4

2. **Form field container**: Replace .form-field with space-y-2

3. **Form labels**: Replace .form-label with:
   text-sm font-medium text-foreground
   - Required asterisk: text-destructive

4. **Text inputs**: Replace .form-input with:
   w-full px-3 py-2 rounded-md border border-input bg-background text-foreground
   placeholder:text-muted-foreground
   focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2
   disabled:opacity-50 disabled:cursor-not-allowed

5. **Input error state**: Replace .form-input-error with:
   border-destructive focus:ring-destructive

6. **Error message**: Replace .form-error with:
   text-sm text-destructive

7. **Textarea**: Replace .form-textarea with same styling as input plus:
   min-h-[80px] resize-y

8. **Form hint**: Replace .form-hint with:
   text-xs text-muted-foreground

9. **Tags container**: Replace .tags-input-container with:
   flex flex-wrap gap-2 p-2 rounded-md border border-input bg-background min-h-[42px]

10. **Tags list**: Replace .tags-list with:
    flex flex-wrap gap-1.5

11. **Individual tag**: Replace .tag with:
    inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-sm
    bg-primary/10 text-primary

12. **Tag remove button**: Replace .tag-remove with:
    size-4 rounded-full hover:bg-primary/20 flex items-center justify-center
    Use X icon from lucide-react with size-3

13. **Tags input**: Replace .tags-input with:
    flex-1 min-w-[120px] bg-transparent border-0 outline-none text-sm
    placeholder:text-muted-foreground

14. **Form row** (for side-by-side fields): Replace .form-row with:
    flex gap-4

15. **Select**: Replace .form-select with:
    w-full px-3 py-2 rounded-md border border-input bg-background text-foreground
    focus:outline-none focus:ring-2 focus:ring-ring cursor-pointer

Import X from "lucide-react", cn from "@/lib/utils".
Optionally import Input from "@/components/ui/input" if available and use that instead of manual styling.
  </action>
  <verify>Run pnpm exec tsc --noEmit. View BasicInfoSection - inputs have proper border and focus rings, error states show red border, tags display as purple pills with remove buttons, labels are styled.</verify>
  <done>BasicInfoSection has styled form fields with proper borders, focus states, error styling, and purple tag pills. No custom CSS classes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Visit /projects/new page
2. Verify header has FolderPlus icon and styled text
3. Verify accordion sections have card backgrounds
4. Verify section status indicators show proper icons (check for complete, circle for incomplete)
5. Verify form inputs have proper styling with focus rings
6. Verify tags appear as purple pills with X remove buttons
7. Verify checklist sidebar shows progress bar and section list
8. Verify action buttons are shadcn styled (ghost for cancel, default for submit)
9. Visit /projects/:id/edit - verify loading skeleton and error states
10. Toggle dark/light mode - all elements should adapt correctly
</verification>

<success_criteria>
- ProjectCreatePage/ProjectEditPage have styled headers with appropriate icons
- Loading states use Skeleton components
- Error states have centered card with icon and back button
- ProjectForm accordion items have bg-card backgrounds
- Section status shows Check/Circle/AlertCircle icons with semantic colors
- Chevron rotates 180deg when section is open
- Action buttons use shadcn Button (ghost for cancel, default for submit)
- ProjectChecklist has progress bar with purple fill
- Section list items show check/circle icons with proper colors
- Activation requirements show checkmark or X with colors
- BasicInfoSection inputs have proper border-input styling
- Focus states show ring-2 ring-ring
- Error states show border-destructive
- Tags display as purple pills (bg-primary/10 text-primary)
- All components render correctly in both light and dark modes
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create .planning/phases/05.2-style-phase-5-screens/05.2-03-SUMMARY.md
</output>

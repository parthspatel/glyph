---
phase: 05.2-style-phase-5-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/ProjectDetailPage.tsx
  - apps/web/src/components/project/ProjectOverview.tsx
  - apps/web/src/components/project/ProjectActivity.tsx
autonomous: true

must_haves:
  truths:
    - "Project detail page uses 75/25 layout (main content 75%, sidebar 25%)"
    - "Stat cards display with semantic border accents (border-l-4 border-l-{color})"
    - "Status badges use semantic colors matching ProjectTable"
    - "Activity feed has timeline style with vertical connecting line"
    - "Activity entries use compact/condensed height"
    - "Loading and error states use proper design tokens"
    - "Sidebar actions use shadcn Button components"
  artifacts:
    - path: "apps/web/src/pages/ProjectDetailPage.tsx"
      provides: "Styled project detail page with 75/25 layout"
      contains: "grid grid-cols|bg-card|Skeleton"
    - path: "apps/web/src/components/project/ProjectOverview.tsx"
      provides: "Styled overview with stat cards and status badges"
      contains: "border-l-4|border-l-primary|border-l-success|bg-card"
    - path: "apps/web/src/components/project/ProjectActivity.tsx"
      provides: "Themed activity feed with timeline design"
      contains: "border-l-2|bg-card|text-muted-foreground"
  key_links:
    - from: "ProjectOverview.tsx"
      to: "QualityStats pattern"
      via: "stat cards"
      pattern: "border-l-4 border-l-(primary|success|info)"
---

<objective>
Style the Project Detail feature (ProjectDetailPage, ProjectOverview, ProjectActivity) using Tailwind classes and design tokens from Phase 3.1.

Purpose: Create a professional project detail view with 75/25 layout, stat cards with semantic borders (matching QualityStats pattern), and a compact timeline activity feed.

Output: Styled components using bg-card, semantic border colors, timeline activity feed, and proper loading/error states.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.2-style-phase-5-screens/05.2-CONTEXT.md
@apps/web/src/index.css
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/src/components/user/QualityStats.tsx (reference for stat cards with border accents)
@apps/web/src/pages/admin/ProjectTypesPage.tsx (reference for loading/empty patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style ProjectDetailPage with 75/25 layout and loading/error states</name>
  <files>apps/web/src/pages/ProjectDetailPage.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities:

1. **Page container**: Replace .page-container with max-w-6xl mx-auto p-6 space-y-6

2. **Loading state**: Use Skeleton components
   Wrap in div with space-y-6
   - Breadcrumb skeleton: Skeleton with h-4 w-48
   - Layout skeleton: div with grid grid-cols-1 lg:grid-cols-4 gap-6
     - Main area (3 cols): Skeleton with h-96
     - Sidebar (1 col): Skeleton with h-64

3. **Error state**: 
   Container: bg-card rounded-lg border p-8 text-center
   - AlertCircle icon from lucide-react with size-12 mx-auto mb-4 text-muted-foreground
   - h2 with text-lg font-semibold text-foreground
   - p with text-muted-foreground mt-2
   - Button with variant="default" and asChild for Link to /projects, mt-4

4. **Breadcrumb navigation**:
   - nav with flex items-center gap-2 text-sm
   - Link to /projects with text-muted-foreground hover:text-foreground
   - Separator: ChevronRight icon with size-4 text-muted-foreground
   - Current page: span with text-foreground font-medium

5. **Main layout** (75/25 split):
   Replace .project-detail-layout with grid grid-cols-1 lg:grid-cols-4 gap-6

6. **Main content area**:
   Replace .project-detail-main with lg:col-span-3

7. **Sidebar**:
   Replace .project-detail-sidebar with space-y-6

8. **Sidebar sections**:
   Replace .sidebar-section with bg-card rounded-lg border p-4
   - Title: h2 with text-sm font-semibold text-foreground mb-3
   - Actions container: space-y-2

9. **Sidebar action buttons**: Use shadcn Button with variant="outline" className="w-full justify-start"
   - Edit: Pencil icon with size-4 mr-2
   - Tasks: ListTodo icon with size-4 mr-2  
   - Clone: Copy icon with size-4 mr-2

Import Skeleton from "@/components/ui/skeleton", Button from "@/components/ui/button", AlertCircle, ChevronRight, Pencil, ListTodo, Copy from "lucide-react".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View /projects/:id - 75/25 layout visible on desktop, loading shows skeletons, error state has centered card with icon/text/button, sidebar has stacked outline buttons.</verify>
  <done>ProjectDetailPage has 75/25 grid layout, skeleton loading states, proper error state with CTA, and styled sidebar with outline action buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Style ProjectOverview with stat cards and semantic borders</name>
  <files>apps/web/src/components/project/ProjectOverview.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities following QualityStats pattern:

1. **Overview container**: Replace .project-overview with space-y-6

2. **Project header section**:
   Replace .project-header with flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4
   - Title section: space-y-1
   - h1: text-2xl font-bold text-foreground
   - Description p: text-muted-foreground

3. **StatusBadge** (same as ProjectTable):
   Define statusColors mapping:
   - draft: "bg-muted text-muted-foreground"
   - active: "bg-success/10 text-success"
   - paused: "bg-warning/10 text-warning"
   - completed: "bg-info/10 text-info"
   - archived: "bg-muted text-muted-foreground"
   Badge: cn("inline-flex items-center px-2.5 py-1 rounded-full text-sm font-medium capitalize", statusColors[status])

4. **Metrics grid with semantic border accents** (following QualityStats pattern):
   Replace .metrics-grid with grid grid-cols-2 md:grid-cols-4 gap-4
   
   Define stats array:
   - Tasks: border-l-4 border-l-primary
   - Completion: border-l-4 border-l-success
   - Active status: border-l-4 border-l-info
   - Data Sources: border-l-4 border-l-warning

5. **MetricCard component**:
   Replace .metric-card with cn("bg-card rounded-lg border p-4", className)
   - Remove .metric-header, just use the icon directly
   - Icon wrapper: text-2xl mb-1 (keep emoji icons or replace with lucide icons)
   - Value: text-2xl font-bold text-foreground
   - Label: text-sm text-muted-foreground

6. **Configuration section**:
   Replace .overview-section with bg-card rounded-lg border p-6
   - Section title h2: text-lg font-semibold text-foreground mb-4

7. **Config grid**:
   Replace .config-grid with grid grid-cols-2 gap-4
   - Each item: space-y-1
   - Label dt: text-sm text-muted-foreground
   - Value dd: text-sm font-medium text-foreground

8. **Status actions section**:
   Container: bg-card rounded-lg border p-6
   - Title: text-lg font-semibold text-foreground mb-4
   - Buttons container: flex flex-wrap gap-3
   - Primary action: Button (default variant)
   - Secondary actions: Button variant="outline"
   - Destructive action (Archive): Button variant="ghost" with text-destructive hover:text-destructive hover:bg-destructive/10

9. **Edit link at bottom**:
   Replace .overview-actions with pt-4
   Use Button variant="outline" with asChild for Link

Import cn from "@/lib/utils", Button from "@/components/ui/button".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectOverview - stat cards have colored left borders (purple, green, blue, amber), status badge shows correct color, config section is styled, action buttons use proper variants.</verify>
  <done>ProjectOverview has stat cards with border-l-4 semantic accents, semantic status badge, styled config grid, and proper button variants for actions.</done>
</task>

<task type="auto">
  <name>Task 3: Style ProjectActivity with timeline design</name>
  <files>apps/web/src/components/project/ProjectActivity.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities to create a compact timeline:

1. **Activity section container**:
   Replace .activity-section with bg-card rounded-lg border p-4

2. **Activity header**:
   Replace .activity-header with flex items-center justify-between mb-4
   - Title h2: text-sm font-semibold text-foreground
   - View all link: text-xs text-primary hover:underline

3. **Activity list with timeline**:
   Replace .activity-list with relative space-y-0
   Add a vertical line using pseudo-element approach:
   - Before the ul, add: div with absolute left-[11px] top-3 bottom-3 w-0.5 bg-border (the timeline line)

4. **Activity item** (compact height):
   Replace .activity-item with relative flex gap-3 py-2 (compact padding)
   - First item: pt-0
   - Last item: pb-0

5. **Activity icon wrapper** (timeline dot):
   Replace .activity-icon-wrapper with:
   relative z-10 flex items-center justify-center size-6 rounded-full bg-muted border-2 border-background
   (This creates a dot that sits on the timeline line)

6. **Activity icon**:
   Replace .activity-icon with text-xs
   Keep emoji icons or convert to smaller lucide icons

7. **Activity content** (compact):
   Replace .activity-content with flex-1 min-w-0

8. **Activity message**:
   Replace .activity-message with text-sm text-foreground leading-tight
   - Actor name span: font-medium

9. **Activity time**:
   Replace .activity-time with text-xs text-muted-foreground mt-0.5

10. **Empty state**:
    Replace .activity-empty with text-center py-4 text-sm text-muted-foreground

11. **Alternative: Simpler timeline without absolute positioning**:
    If the absolute positioning is complex, use a simpler approach:
    - Each activity item: flex gap-3 py-2
    - Icon column: flex flex-col items-center
      - Icon: size-6 rounded-full bg-muted flex items-center justify-center text-xs
      - Line below (except last): flex-1 w-0.5 bg-border mt-1
    - Content column: flex-1

Import cn from "@/lib/utils" if needed.
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectActivity - timeline has vertical connecting line, activity items are compact, icons sit on the timeline, timestamps are small and muted.</verify>
  <done>ProjectActivity has timeline design with vertical line connecting items, compact item heights, proper icon placement, and styled timestamps. Card background with proper header.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Visit /projects/:id page (existing project)
2. Verify 75/25 layout - main content takes 3/4, sidebar takes 1/4
3. Verify stat cards have colored left borders (purple, green, blue, amber)
4. Verify status badge matches color scheme (active=green, paused=amber, etc.)
5. Verify sidebar has stacked outline buttons with icons
6. Verify activity feed has timeline design with vertical line
7. Verify activity items are compact (not too much vertical space)
8. Check loading state - shows skeleton placeholders
9. Check error state - centered card with icon and back button
10. Toggle dark/light mode - all elements should adapt correctly
</verification>

<success_criteria>
- ProjectDetailPage uses grid grid-cols-4 for 75/25 layout
- Loading state shows Skeleton components
- Error state has centered card with AlertCircle icon and CTA button
- Sidebar actions use Button variant="outline" with icons
- ProjectOverview stat cards have border-l-4 with semantic colors
- Status badge uses semantic colors (success, warning, info, muted)
- Config section displays in styled card with grid layout
- Action buttons use appropriate variants (default, outline, ghost+destructive)
- ProjectActivity has vertical timeline line connecting items
- Activity items use compact py-2 spacing
- All components render correctly in both light and dark modes
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create .planning/phases/05.2-style-phase-5-screens/05.2-02-SUMMARY.md
</output>

---
phase: 05.2-style-phase-5-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/ProjectsPage.tsx
  - apps/web/src/components/project/ProjectTable.tsx
  - apps/web/src/components/project/ProjectFilters.tsx
  - apps/web/src/components/project/BulkActions.tsx
autonomous: true

must_haves:
  truths:
    - "Projects page displays with proper header styling and purple create button"
    - "Project table has zebra striping with even:bg-muted/30 pattern"
    - "Selected rows show purple highlight with bg-primary/5"
    - "Status badges use semantic colors: draft=gray, active=green, paused=amber, completed=blue, archived=gray"
    - "Progress displays as plain text percentage, not progress bar"
    - "Pagination controls use shadcn Button with outline variant"
    - "Empty and loading states match ProjectTypesPage patterns"
  artifacts:
    - path: "apps/web/src/pages/ProjectsPage.tsx"
      provides: "Styled projects list page with themed header and pagination"
      contains: "bg-card|Button|Skeleton"
    - path: "apps/web/src/components/project/ProjectTable.tsx"
      provides: "Themed data table with zebra stripes and status badges"
      contains: "even:bg-muted/30|bg-primary/5|bg-success|bg-warning"
    - path: "apps/web/src/components/project/ProjectFilters.tsx"
      provides: "Styled filter controls with design tokens"
      contains: "bg-card|border-input|rounded-md"
    - path: "apps/web/src/components/project/BulkActions.tsx"
      provides: "Themed bulk actions bar"
      contains: "bg-card|Button"
  key_links:
    - from: "ProjectTable.tsx"
      to: "design tokens"
      via: "Tailwind classes"
      pattern: "bg-(success|warning|info|muted)"
---

<objective>
Style the Projects List feature (ProjectsPage, ProjectTable, ProjectFilters, BulkActions) using Tailwind classes and design tokens from Phase 3.1.

Purpose: Make the projects list visually consistent with the royal purple design system and Phase 4.2 patterns (UserTable zebra stripes, semantic badges).

Output: Styled components using bg-card, semantic status colors, zebra stripes, purple selection states, and text-only progress percentages.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05.2-style-phase-5-screens/05.2-CONTEXT.md
@apps/web/src/index.css
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/src/components/admin/UserTable.tsx (reference for zebra stripes, selection pattern)
@apps/web/src/pages/admin/ProjectTypesPage.tsx (reference for empty/loading states)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style ProjectTable with zebra stripes, semantic status badges, and text progress</name>
  <files>apps/web/src/components/project/ProjectTable.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities following the UserTable pattern:

1. **Remove ProgressBar component**: Replace with plain text percentage display
   - Remove the ProgressBar function entirely
   - In progress column cell, return: const pct = total > 0 ? Math.round((completed / total) * 100) : 0; return a span with className="text-muted-foreground" containing {pct}%

2. **StatusBadge with semantic colors**:
   Define statusColors mapping:
   - draft: "bg-muted text-muted-foreground"
   - active: "bg-success/10 text-success"
   - paused: "bg-warning/10 text-warning"
   - completed: "bg-info/10 text-info"
   - archived: "bg-muted text-muted-foreground"
   
   StatusBadge should use: cn("inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium capitalize", statusColors[status])

3. **Table container**: Replace .table-wrapper with overflow-x-auto

4. **Column toggle toolbar**: 
   - Container: flex items-center gap-2 mb-3 text-sm text-muted-foreground
   - Label: font-medium
   - Checkbox items: flex items-center gap-1.5 cursor-pointer

5. **Table element**: Replace .data-table with w-full text-sm

6. **Table header**:
   - thead: bg-muted/50
   - th: px-4 py-3 text-left font-medium text-muted-foreground
   - Sortable: Add cursor-pointer hover:bg-muted transition-colors select-none
   - Sort indicator: Wrap in div with flex items-center gap-1, use ChevronUp/ChevronDown from lucide-react with size-4

7. **Table body rows with zebra stripes**:
   tr className should use cn() with:
   - "border-b border-border transition-colors cursor-pointer"
   - "even:bg-muted/30"
   - "hover:bg-muted/50"
   - row.getIsSelected() && "bg-primary/5"

8. **Table cells**: td should have px-4 py-3

9. **Checkbox styling**: cn("size-4 rounded border-input cursor-pointer", "accent-primary", "focus:ring-2 focus:ring-ring focus:ring-offset-2")

10. **Name link**: font-medium text-foreground hover:text-primary hover:underline

11. **Tasks column**: text-muted-foreground

12. **Date column**: text-muted-foreground

13. **Owner column**: text-muted-foreground

14. **Actions cell**:
    - Container: flex items-center justify-end
    - Use shadcn Button with variant="ghost" size="sm"

15. **Empty state**: text-center py-8 text-muted-foreground

Import cn from "@/lib/utils", Button from "@/components/ui/button", ChevronUp/ChevronDown from "lucide-react".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectTable - alternating row backgrounds visible, status badges show correct semantic colors, progress is plain text percentage, selected rows have purple tint.</verify>
  <done>ProjectTable has zebra stripes (even:bg-muted/30), semantic status badges, purple selection (bg-primary/5), and text-only progress. No progress bar component.</done>
</task>

<task type="auto">
  <name>Task 2: Style ProjectsPage with header, loading/empty states, and pagination</name>
  <files>apps/web/src/pages/ProjectsPage.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utilities following ProjectTypesPage pattern:

1. **Page container**: Replace .page-container with max-w-6xl mx-auto p-6 space-y-6

2. **Page header**: Use flex items-center justify-between. Left side has flex items-center gap-3 with:
   - FolderKanban icon with size-8 text-primary
   - div containing h1 (text-2xl font-bold text-foreground) and p (text-muted-foreground)
   Right side has Button with Link inside using asChild pattern, Plus icon with size-4 mr-2
   Import FolderKanban, Plus from "lucide-react", Button from "@/components/ui/button".

3. **Error banner**: Replace .error-banner with bg-destructive/10 text-destructive border border-destructive/20 rounded-md p-4

4. **Loading state**: Use Skeleton component from @/components/ui/skeleton
   Wrap in div with bg-card rounded-lg border p-6 space-y-4
   Map 5 items, each with flex items-center gap-4 containing Skeleton elements

5. **Table card wrapper**: Replace .table-card with bg-card rounded-lg border overflow-hidden

6. **Pagination container**: Replace .pagination with flex items-center justify-between pt-4

7. **Pagination info**: text-sm text-muted-foreground

8. **Pagination buttons**: flex items-center gap-2
   Use Button with variant="outline" size="sm" for Previous/Next
   Page indicator span with text-sm text-muted-foreground px-2

9. **Empty state** (when data.items.length === 0):
   Wrap in bg-card rounded-lg border
   Inner div with text-center py-12 text-muted-foreground
   FolderKanban icon with size-12 mx-auto mb-3 opacity-50
   p with text-lg font-medium for title
   p with text-sm mt-1 for subtitle
   Button with Link using asChild pattern, mt-4
  </action>
  <verify>Run pnpm exec tsc --noEmit. View /projects - header has icon and purple button, loading shows skeleton pulses, empty state has centered icon/text/CTA, pagination uses outline buttons.</verify>
  <done>ProjectsPage has styled header with icon/button, skeleton loading states, proper empty state with CTA, and themed pagination. No custom CSS classes.</done>
</task>

<task type="auto">
  <name>Task 3: Style ProjectFilters and BulkActions</name>
  <files>apps/web/src/components/project/ProjectFilters.tsx, apps/web/src/components/project/BulkActions.tsx</files>
  <action>
**ProjectFilters.tsx:**

1. **Filter container**: Replace .project-filters with flex flex-wrap items-center gap-4

2. **View toggle group**: Use div with flex items-center rounded-lg border border-input bg-muted/30 p-0.5
   Map over views, each button uses cn() with:
   - "px-3 py-1.5 text-sm font-medium rounded-md transition-colors"
   - Active: "bg-background text-foreground shadow-sm"
   - Inactive: "text-muted-foreground hover:text-foreground"

3. **Search input**:
   - Wrapper: relative flex-1 max-w-sm
   - Icon: Use Search from lucide-react with absolute left-3 top-1/2 -translate-y-1/2 size-4 text-muted-foreground
   - Input: w-full pl-9 pr-8 py-2 rounded-md border border-input bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2
   - Clear button: absolute right-2 top-1/2 -translate-y-1/2 size-5 rounded-full hover:bg-muted flex items-center justify-center text-muted-foreground hover:text-foreground
   - Use X icon from lucide-react

4. **Status filter select**: px-3 py-2 rounded-md border border-input bg-background text-foreground focus:outline-none focus:ring-2 focus:ring-ring cursor-pointer

Import cn from "@/lib/utils", X, Search from "lucide-react".

**BulkActions.tsx:**

1. **Bar container**: fixed bottom-0 left-0 right-0 bg-card border-t border-border shadow-lg z-40 animate-in slide-in-from-bottom-2

2. **Content wrapper**: max-w-6xl mx-auto px-6 py-3 flex items-center justify-between

3. **Selection count**: text-sm font-medium text-foreground

4. **Buttons container**: flex items-center gap-3

5. **Status select**: px-3 py-1.5 rounded-md border border-input bg-background text-sm focus:outline-none focus:ring-2 focus:ring-ring cursor-pointer

6. **Delete button**: Use Button with variant="destructive" size="sm"

7. **Cancel button**: Use Button with variant="ghost" size="sm"

Import Button from "@/components/ui/button".
  </action>
  <verify>Run pnpm exec tsc --noEmit. View ProjectFilters - view toggle has active state highlighting, search has proper focus ring, select is styled. BulkActions bar appears at bottom with proper card background and styled buttons.</verify>
  <done>ProjectFilters has segmented view toggle, styled search with icon, themed select. BulkActions has fixed card bar with proper button variants. No custom CSS classes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Visit /projects page
2. Verify page header has FolderKanban icon and purple "New Project" button
3. Verify table has subtle zebra stripes (even:bg-muted/30)
4. Verify status badges: draft=gray, active=green, paused=amber, completed=blue, archived=gray
5. Verify progress shows as plain text (e.g., "45%") not a progress bar
6. Select rows - verify purple tint appears
7. Check filter controls - view toggle, search, status dropdown all styled
8. Check bulk actions bar appears when rows selected
9. Toggle dark/light mode - all elements should adapt correctly
10. No custom CSS class names remain (.btn, .status-badge, .progress-bar, etc.)
</verification>

<success_criteria>
- ProjectsPage displays with themed header, icon, and purple button
- ProjectTable shows zebra stripes with even:bg-muted/30
- Selected rows have bg-primary/5 purple tint
- Status badges use semantic colors (success for active, warning for paused, etc.)
- Progress displays as plain text percentage, no ProgressBar component
- Pagination uses shadcn Button with variant="outline"
- Loading state uses Skeleton components with pulse animation
- Empty state has centered icon, text, and CTA button
- ProjectFilters has segmented view toggle and styled inputs
- BulkActions bar is fixed at bottom with proper styling
- All components render correctly in both light and dark modes
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create .planning/phases/05.2-style-phase-5-screens/05.2-01-SUMMARY.md
</output>

---
phase: 04.2-style-phase-4-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/UserProfilePage.tsx
  - apps/web/src/components/user/SkillBadges.tsx
  - apps/web/src/components/user/QualityStats.tsx
autonomous: true

must_haves:
  truths:
    - "User profile page displays with proper card backgrounds and borders"
    - "SkillBadges show status colors using semantic tokens (success, warning, destructive)"
    - "QualityStats uses themed stat cards with proper semantic colors"
    - "All elements render correctly in both light and dark modes"
    - "Purple accent visible only on interactive elements (buttons, links)"
  artifacts:
    - path: "apps/web/src/pages/UserProfilePage.tsx"
      provides: "Styled user profile page"
      contains: "bg-card|bg-background|text-foreground"
    - path: "apps/web/src/components/user/SkillBadges.tsx"
      provides: "Themed skill badges with status colors"
      contains: "bg-success|bg-warning|bg-destructive"
    - path: "apps/web/src/components/user/QualityStats.tsx"
      provides: "Themed quality stats display"
      contains: "bg-card|text-muted-foreground"
  key_links:
    - from: "SkillBadges.tsx"
      to: "design tokens"
      via: "Tailwind classes"
      pattern: "bg-(success|warning|destructive|info)"
---

<objective>
Style the User Profile feature (UserProfilePage, SkillBadges, QualityStats) using Tailwind classes and design tokens from Phase 3.1.

Purpose: Make user profile screens visually consistent with the royal purple design system while maintaining professional, subtle aesthetics.

Output: Three styled components using bg-card, text-foreground, semantic colors, and shadcn/ui Button component.
</objective>

<execution_context>
@/Users/parthpatel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parthpatel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.2-style-phase-4-screens/04.2-CONTEXT.md
@apps/web/src/index.css
@apps/web/src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style UserProfilePage with design tokens</name>
  <files>apps/web/src/pages/UserProfilePage.tsx</files>
  <action>
Replace all custom CSS classes with Tailwind utility classes using design tokens:

1. **Page container**: Replace `.page-container` with `max-w-4xl mx-auto p-6`
2. **Loading skeleton**: Use shadcn Skeleton component or `animate-pulse bg-muted rounded`
3. **Error banner**: Replace `.error-banner` with `bg-destructive/10 text-destructive border border-destructive/20 rounded-md p-4`
4. **Profile header**: 
   - `.profile-header` -> `flex gap-6 items-start`
   - `.profile-avatar` -> `size-20 rounded-full bg-muted flex items-center justify-center text-2xl font-semibold`
5. **Role/status badges**:
   - Use `inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium`
   - Role colors: admin=`bg-primary/10 text-primary`, manager=`bg-info/10 text-info`, annotator=`bg-muted text-muted-foreground`
   - Status: active=`bg-success/10 text-success`, inactive=`bg-muted text-muted-foreground`
6. **Form sections**: 
   - `.form-field` -> `space-y-2`
   - Use Input component from shadcn or style inputs with `bg-background border-input rounded-md px-3 py-2`
7. **Buttons**: Import and use shadcn Button component
   - Primary: `<Button>Save</Button>`
   - Secondary: `<Button variant="secondary">Cancel</Button>`
   - Link style: `<Button variant="link">Edit Profile</Button>`
8. **Profile sections**: Replace `.profile-section` with `bg-card rounded-lg border p-6 space-y-4`
9. **Contact list**: Style `.contact-item` with `flex justify-between py-2 border-b border-border last:border-0`

Import cn from "@/lib/utils" and Button from "@/components/ui/button".
  </action>
  <verify>Run `pnpm exec tsc --noEmit` - no type errors. Check UserProfilePage in browser - proper card backgrounds, purple buttons, correct text colors in both themes.</verify>
  <done>UserProfilePage uses Tailwind design tokens with no custom CSS classes. Cards have bg-card, text uses text-foreground/text-muted-foreground, buttons are purple.</done>
</task>

<task type="auto">
  <name>Task 2: Style SkillBadges and QualityStats components</name>
  <files>apps/web/src/components/user/SkillBadges.tsx, apps/web/src/components/user/QualityStats.tsx</files>
  <action>
**SkillBadges.tsx:**

1. Replace statusColors mapping with semantic Tailwind classes:
   ```typescript
   const statusColors: Record<string, string> = {
     active: "bg-success/10 text-success border-success/20",
     soft_expired: "bg-warning/10 text-warning border-warning/20", 
     hard_expired: "bg-destructive/10 text-destructive border-destructive/20",
     never_expires: "bg-info/10 text-info border-info/20",
   };
   ```

2. Style skill badges container: `flex flex-wrap gap-2`

3. Style each skill badge button:
   `inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-sm font-medium border cursor-pointer transition-colors hover:opacity-80`

4. Style modal overlay: `fixed inset-0 bg-black/50 flex items-center justify-center z-50`

5. Style modal content: `bg-card rounded-lg border shadow-lg p-6 max-w-md w-full mx-4`

6. Style skill details list: `space-y-3 mt-4`

7. Style detail rows: `flex justify-between items-center`

8. Import and use Button component for close button

**QualityStats.tsx:**

1. Update stat card className mapping:
   ```typescript
   const stats = [
     { label: "Total Tasks", value: quality.total_tasks, className: "border-l-4 border-l-primary" },
     { label: "Completed", value: quality.completed_tasks, className: "border-l-4 border-l-success" },
     { label: "Errors", value: quality.error_count, className: "border-l-4 border-l-destructive" },
     { label: "Avg Time", value: `${quality.avg_time_seconds}s`, className: "border-l-4 border-l-info" },
   ];
   ```

2. Style stats grid: `grid grid-cols-2 md:grid-cols-4 gap-4`

3. Style stat cards: `bg-card rounded-lg border p-4 ${stat.className}`

4. Style stat value: `text-2xl font-bold text-foreground`

5. Style stat label: `text-sm text-muted-foreground`

6. Style score bar:
   - Container: `mt-6 space-y-2`
   - Header: `flex justify-between text-sm`
   - Track: `h-2 bg-muted rounded-full overflow-hidden`
   - Fill: `h-full bg-primary transition-all` with width from score

7. Style additional scores: `grid grid-cols-2 gap-4 mt-4`

8. Style score items: `text-center`
   - Label: `text-sm text-muted-foreground`
   - Value: `text-lg font-semibold`

Import cn from "@/lib/utils".
  </action>
  <verify>Run `pnpm exec tsc --noEmit`. View SkillBadges - badges show green for active, yellow for expiring, red for expired. View QualityStats - stat cards have colored left borders, score bar shows purple fill.</verify>
  <done>SkillBadges uses semantic status colors (success/warning/destructive). QualityStats uses themed stat cards with purple score bar. Both work in dark/light modes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Visit /users/:id page
2. Verify profile header has proper card styling
3. Verify skill badges show appropriate status colors
4. Verify quality stats display with semantic colors
5. Toggle dark/light mode - all elements should adapt correctly
6. Buttons should be purple (primary color)
7. No custom CSS class names remain (no `.btn`, `.modal-overlay`, etc.)
</verification>

<success_criteria>
- UserProfilePage displays with bg-card sections and text-foreground text
- SkillBadges use bg-success/bg-warning/bg-destructive for status indication
- QualityStats shows stat cards with colored borders and purple progress bar
- All components render correctly in both light and dark modes
- Purple accent appears only on buttons and interactive elements
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-style-phase-4-screens/04.2-01-SUMMARY.md`
</output>

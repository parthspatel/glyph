---
phase: 09.2-project-overview-modules-ui
plan: 07
type: execute
wave: 4
depends_on: ["09.2-06"]
files_modified:
  - apps/web/src/pages/ProjectsPage.tsx
  - apps/web/src/pages/TeamsPage.tsx
  - apps/web/src/pages/TeamDetailPage.tsx
  - apps/web/src/pages/UserProfilePage.tsx
  - apps/web/src/pages/HomePage.tsx
  - apps/web/src/index.css
autonomous: false

must_haves:
  truths:
    - "All pages use consistent p-6 page padding"
    - "Cards use consistent p-4 padding"
    - "Grid gaps are consistently gap-4"
    - "Loading states use skeleton components"
    - "Interactive elements have hover feedback"
    - "Dark mode colors work correctly"
  artifacts:
    - path: "apps/web/src/pages/ProjectsPage.tsx"
      provides: "Polished projects list page"
    - path: "apps/web/src/pages/TeamsPage.tsx"
      provides: "Polished teams list page"
  key_links:
    - from: "All pages"
      to: "index.css design tokens"
      via: "Tailwind classes using CSS variables"
      pattern: "bg-card|text-foreground|text-muted-foreground"
---

<objective>
Conduct platform-wide UI polish audit ensuring consistent spacing, loading states, hover feedback, and dark mode compatibility across all screens.

Purpose: Delivers a polished, professional feel across the entire platform with no visual inconsistencies or "jank".

Output: Consistent UI polish across all major screens.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/09.2-project-overview-modules-ui/09.2-RESEARCH.md
@.planning/phases/09.2-project-overview-modules-ui/09.2-06-SUMMARY.md

@apps/web/src/index.css
@apps/web/src/pages/ProjectsPage.tsx
@apps/web/src/pages/TeamsPage.tsx
@apps/web/src/pages/TeamDetailPage.tsx
@apps/web/src/pages/UserProfilePage.tsx
@apps/web/src/pages/HomePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix spacing consistency</name>
  <files>
    apps/web/src/pages/ProjectsPage.tsx
    apps/web/src/pages/TeamsPage.tsx
    apps/web/src/pages/TeamDetailPage.tsx
    apps/web/src/pages/UserProfilePage.tsx
    apps/web/src/pages/HomePage.tsx
  </files>
  <action>
Audit all major pages for spacing consistency per research standards:

**Spacing tokens to enforce:**
| Element | Token |
|---------|-------|
| Page container padding | `p-6` |
| Section vertical gap | `space-y-6` |
| Card internal padding | `p-4` or `p-6` (headers p-6, content cards p-4) |
| Grid/flex gap | `gap-4` |
| Form field gap | `space-y-4` |
| Button internal gap | `gap-2` |

**For each page, check and fix:**
1. Root container has `p-6` padding
2. Sections separated by `space-y-6`
3. Cards use consistent padding
4. Grids use `gap-4`

**Common issues to fix:**
- Missing page padding (some pages have `p-4`, others `p-8`)
- Inconsistent card padding
- Hardcoded pixel values instead of spacing scale
- Missing max-width constraints (`max-w-7xl mx-auto`)

Document all changes made.
  </action>
  <verify>
Visual inspection: All pages have consistent spacing
No hardcoded pixel values for padding/margins (except specific cases)
Tailwind classes follow spacing scale (4, 6, 8...)
  </verify>
  <done>
All major pages use consistent spacing tokens per design standards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing loading states and hover feedback</name>
  <files>
    apps/web/src/pages/ProjectsPage.tsx
    apps/web/src/pages/TeamsPage.tsx
    apps/web/src/pages/HomePage.tsx
    apps/web/src/components/project/ProjectTable.tsx
  </files>
  <action>
Audit and add missing loading states and hover feedback:

**Loading states:**
- Every page with data fetching should show Skeleton while loading
- Match skeleton structure to actual content structure
- No blank white screens during load

**Check each page:**
```tsx
if (isLoading) {
  return <PageSkeleton />; // Structured skeleton, not just spinner
}
```

**Hover feedback for interactive elements:**
- Clickable cards: `hover:shadow-md hover:-translate-y-0.5 transition-all duration-150`
- Table rows: `hover:bg-muted/50`
- Buttons already have hover states from shadcn

**Common patterns to add:**
```tsx
// Card hover
<Card className="cursor-pointer hover:shadow-md hover:-translate-y-0.5 transition-all duration-150">

// Table row hover
<tr className="hover:bg-muted/50 transition-colors">

// Link cards
<Link className="block rounded-lg border p-4 hover:border-primary/50 hover:bg-muted/30 transition-colors">
```

Add transition classes where missing.
  </action>
  <verify>
All data-fetching pages show skeletons while loading
Interactive elements have visible hover state changes
Transitions are smooth (150-200ms)
  </verify>
  <done>
All pages have loading skeletons and interactive elements have hover feedback.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of UI polish</name>
  <what-built>
Platform-wide UI polish including:
- Consistent spacing (p-6 pages, p-4 cards, gap-4 grids)
- Loading skeletons on all data pages
- Hover states on all interactive elements
- Dark mode compatibility
  </what-built>
  <how-to-verify>
1. Start dev server: `cd apps/web && pnpm dev`
2. Check each page in LIGHT mode:
   - /projects - list page spacing, table row hovers
   - /projects/:id - module grid spacing, card hovers
   - /teams - list spacing
   - /teams/:id - detail page spacing
   - / (home) - dashboard spacing
3. Toggle to DARK mode (use theme switcher or system preference)
4. Repeat check for all pages - verify:
   - No white/light colored backgrounds that should be dark
   - Text is readable (sufficient contrast)
   - Borders are visible but subtle
5. Test loading states:
   - Refresh each page, verify skeleton appears briefly
   - Throttle network in DevTools, verify skeleton shows
6. Test responsive:
   - Resize browser to mobile width
   - Verify padding adjusts appropriately
   - Verify grids collapse to single column
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- All pages pass spacing audit
- Loading states exist on all data-fetching pages
- Hover feedback visible on all interactive elements
- Dark mode works without visual bugs
- Human verification confirms polish quality
</verification>

<success_criteria>
- Consistent p-6 page padding across all screens
- Consistent p-4/p-6 card padding
- Consistent gap-4 grid spacing
- Loading skeletons on all data pages
- Hover states on all clickable elements
- Dark mode fully functional
- Human approval of visual polish
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-project-overview-modules-ui/09.2-07-SUMMARY.md`
</output>

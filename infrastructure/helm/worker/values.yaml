# Glyph Worker - Default Values

replicaCount: 1

image:
  repository: glyph-worker
  pullPolicy: IfNotPresent
  tag: ""

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"

env:
  RUST_LOG: info
